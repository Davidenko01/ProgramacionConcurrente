package TP5.EJ6;

import java.util.concurrent.Semaphore;

public class Pista {
    /*
    private Semaphore torreDeControl = new Semaphore(0);
    private Semaphore pista = new Semaphore(0);
    private Semaphore permiso = new Semaphore(0);
    private Semaphore permisoDespegue = new Semaphore(0);
    private Semaphore permisosAterrizaje = new Semaphore(10);

    public Pista() {
    }

    public void realizarAccion(String laAccion) {
    }

    public void solicitarPermiso(String laAccion) {
        mutex.acquire();
        torreDeControl.release(1);
    }
    public void consederPermiso() {
        torreDeControl.acquire(1);

    }

     */
}
